import test from 'node:test';import assert from 'node:assert/strict';import {createPreparedImageSource} from '../features/capture/prepared-image-source.js';
test('prepared image decode aborts promptly and closes a late bitmap',async()=>{const c=new AbortController();let closed=0;const factory=()=>new Promise(r=>setTimeout(()=>r({width:10,height:10,close(){closed++}}),60));const p=createPreparedImageSource({},{bitmapFactory:factory,timeoutMs:1000,signal:c.signal});setTimeout(()=>c.abort(),5);await assert.rejects(p,e=>e.code==='IMAGE_DECODE_ABORTED');await new Promise(r=>setTimeout(r,80));assert.equal(closed,1)});
