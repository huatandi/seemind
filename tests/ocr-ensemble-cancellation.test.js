import test from 'node:test';import assert from 'node:assert/strict';import {runOcrEnsemble} from '../core/ocr/ocr-ensemble.js';
test('ensemble abort stops fallback engines immediately',async()=>{const c=new AbortController();let second=0;const first={id:'a',providerType:'local',recognize:async(_i,o)=>new Promise((_,reject)=>{o.signal.addEventListener('abort',()=>{const e=new Error('OCR_ABORTED');e.code='OCR_ABORTED';reject(e)},{once:true})})};const two={id:'b',providerType:'local',recognize:async()=>{second++;return{text:'x',confidence:1,engineId:'b'}}};const p=runOcrEnsemble({candidates:[{ocrInput:{},planId:'p'}],engines:[first,two],signal:c.signal,perRecognitionTimeoutMs:5000});setTimeout(()=>c.abort(),10);await assert.rejects(p,e=>e.code==='OCR_ABORTED');assert.equal(second,0)});
