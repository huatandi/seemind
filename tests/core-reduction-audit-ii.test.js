import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
import {createTeacherOutcomeStore,learnTeacherOutcome,routeIntelligenceGap} from '../core/orchestration/intelligence-gap-router.js';
const provider=id=>({id,getProfile:()=>({capabilities:[{capability:'visual_reasoning',score:1}],privacyModes:['cloud'],latencyClass:'medium',reliabilityScore:.8,evidenceScore:.8,historicalSuccess:.5}),healthCheck:async()=>({status:'ready'})});
test('good verified-outcome learning survives reduction behind canonical teacher surface',async()=>{const s=createTeacherOutcomeStore();for(let i=0;i<6;i++){learnTeacherOutcome(s,{providerId:'b',capabilities:['visual_reasoning'],taskKind:'visual',technicalOk:true,verified:true});learnTeacherOutcome(s,{providerId:'a',capabilities:['visual_reasoning'],taskKind:'visual',technicalOk:true,userCorrected:true})}const x=await routeIntelligenceGap({gap:{kind:'visual',requiredCapabilities:['visual_reasoning']},providers:[provider('a'),provider('b')],outcomeStore:s});assert.equal(x.selected.provider.id,'b')});
test('core lifecycle document protects useful dormant modules from mechanical deletion',()=>{const text=fs.readFileSync('core/CORE_LIFECYCLE.md','utf8');assert.match(text,/zero production imports is a review signal, not a deletion instruction/i);assert.match(text,/task-aware-authority\.js/);assert.equal(fs.existsSync('core/orchestration/specialist-selector.js'),false)});
